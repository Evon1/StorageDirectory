<% declare_content_for %>

<% display_message flash %>

<h2><%= user_account_title %></h2>

<div class="right framed">
	<%= link_to image_tag(@user.profile_image.image.url(:thumb), :alt => @user.name), @user.profile_image if @user.image.exists? %>
</div>

<div class="round box framed">
	<p>
	  <b>Login:</b>
	  <%=h @user.name %>
	</p>

	<p>
	  <b>Email:</b>
	  <%=h @user.email %>
	</p>

	<p>
	  <b>Login count:</b>
	  <%=h @user.login_count %>
	</p>

	<p>
	  <b>Last request at:</b>
	  <%=h @user.last_request_at %>
	</p>

	<p>
	  <b>Last login at:</b>
	  <%=h @user.last_login_at %>
	</p>

	<p>
	  <b>Current login at:</b>
	  <%=h @user.current_login_at %>
	</p>

	<p>
	  <b>Last login ip:</b>
	  <%=h @user.last_login_ip %>
	</p>

	<p>
	  <b>Current login ip:</b>
	  <%=h @user.current_login_ip %>
	</p>

	<p class="buttons">
		<%= link_to 'Edit', edit_user_path %>
	</p>
</div>